name: portfolio-ko

services:
  portfolio-ko:
    build: ./project
    ports:
      - "8080:80"
    volumes:
      - ${CONFIG_FILE:-./config/public/config.json}:/usr/share/nginx/html/config.json
      - ${ASSETS_DIR:-./config/public/assets}:/usr/share/nginx/html/assets
    restart: always
